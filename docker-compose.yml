version: '3.8'

services:
  app:
    build: .
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - VITE_API_BASE_URL=http://localhost:5000/api
    volumes:
      - ./uploads:/app/uploads
    restart: unless-stopped
